import React from 'react';
import { 
    Redirect,
} from "react-router-dom";

import Form from '../../components/form';

function Login () {
    return (
        <div>
            <Form />
        </div>
    );
}

export default Login; 